<h1>Burgers</h1>

<h2>Burgers you have not eaten.</h2>

<ul>
    {{#each burger}}
        {{#unless devoured}}
            {{> burgers/burger-block devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that you have eaten.</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block sleep=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
    
    <div class="form-group">
        <label for="bu">Burger Name:</label>
        <input type="text" id="bu" name="name">
    </div>

    <div class="form-group">
        <label for="devoured">Devoured?</label><br>
        <input type="eaten" name="devoured" value="1" checked> Devoured! <br>
        <input type="eaten" name="devoured" value="0"> Needs to be Eat
    </div>

    <button type="submit">Add Burger</button>
</form>